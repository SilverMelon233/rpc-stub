// Generated by Microsoft TypeSpec

// DemoService — Echo + Health
// Simple RPC service used to demonstrate the full TypeSpec → proto → gRPC pipeline.
syntax = "proto3";

package demo.v1;

import "google/protobuf/empty.proto";

message EchoRequest {
  string message = 1; // The message to echo back.
}

message EchoResponse {
  string message = 1; // The echoed message.
}

message HealthRequest {}

message HealthResponse {
  string status = 1; // "ok" when the service is healthy.
}

service DemoService {
  // Echo a message back to the caller.
  rpc Echo(EchoRequest) returns (EchoResponse);
  // Return the current health status of this service.
  rpc Health(google.protobuf.Empty) returns (HealthResponse);
}
