[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rpc-demo-stub"
version = "0.1.0"
dependencies = ["grpcio>=1.68.0", "protobuf>=5.0.0"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["demo*", "demo_stub*"]
